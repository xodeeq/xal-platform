# Lessons log — platform repo

A running ledger of pitfalls, workarounds, repeated explanations, and consequential
decisions — so none of them evaporate at the end of a session. This is the platform repo's
**own** ledger (eating the dog food: `spec/session-ritual.md` requires one per repo, and
`scaffold/docs/lessons.md` seeds it for new services). Service-specific lessons belong in
that service's ledger, not here.

**The loop: capture → review → promote.**

1. **Capture** (any time): the moment something bites, a workaround goes in, or you find
   yourself explaining the same thing twice, append a one-line entry here with status `open`.
2. **Review** (every session, via `/wrap-session`): walk the open entries and decide each
   one's permanent **home**.
3. **Promote**: move the lesson into the thing that makes it stick, then flip the entry to
   `promoted→<link>`. A lesson that stays `open` for many sessions is a signal it needs a
   decision, not that it should be forgotten.

**Homes** (where a lesson goes to live permanently):

| Home | Use when the lesson is… | Example |
|---|---|---|
| **script** | a mechanical, repeatable task | the sync round-trip → `sync/platform-sync.sh` |
| **skill / command** | a recurring procedure or judgement | end-of-session ritual → `/wrap-session` |
| **guardrail** (CLAUDE.md) | an always-true rule | "no service-language specifics in `spec/`" |
| **ADR** | an architectural decision with tradeoffs | the repo structure + sync model |

> **Never let a temporary workaround outlive the session that created it** without a tracked
> entry here to remove it.

## Format

One line per entry:

```
date | context | lesson | proposed home (script/skill/guardrail/ADR) | status (open / promoted→link)
```

## Entries

```
2026-08-12 | Testing a new gate against deliberate violations found three bugs IN THE GATE | The four platform gates all passed on first run — and three of them were partly broken. Only mutating the repo to force each failure exposed it. (1) **awk has no `\b` word boundary**: POSIX awk reads `\b` as backspace, so the CLAUDE.md leak pattern `\bdotnet\b|…` silently matched NOTHING for its two most important alternatives while appearing to work. grep -E supports `\b`; awk does not, and reusing one pattern across both engines is the trap. (2) **`comm` assumes LEXICALLY sorted input**: line numbers sorted with `sort -n` are in a different order (`"127" < "30"` lexically), so the set difference mis-paired and excused real hits. (3) A paragraph-form citation followed by a bullet list dropped out of its region after the first bullet. A gate that passes proves nothing about whether it can fail. | guardrail (every new gate ships with a proven failure case per rule — mutate, assert non-zero, restore; and never reuse a regex across two engines without checking the metacharacters are supported in both) | promoted→ all three fixed in [#2](https://github.com/xodeeq/xal-platform/pull/2) before merge; the nine violation scenarios are tabulated in `docs/sessions/02.md`. A related process bug: the first test harness restored with `git checkout -- spec sync scaffold`, which destroyed UNCOMMITTED work and invalidated every result after the first — commit before running destructive test harnesses
2026-08-18 | Gate 5's missing CI input is now caught by a gate, not by remembering | When gate 5 (`claude plugin validate --strict`) was added on 2026-08-15 it needed the `claude` CLI and `ci.yml` did not install it, so it failed on its first CI run — the second occurrence of a failure that had blocked auth's production deploys for ~3.5h three days earlier. Both were closed with the same guardrail ("grep every caller before pushing"), and the second happened WITH that guardrail already written, because a guardrail is a memory aid attached to nothing. Root cause: a gate script declares its inputs in code but nowhere as data and cannot see its callers, while a developer's machine satisfies those inputs ambiently (`claude` happens to be on `$PATH`) and CI must supply each one explicitly — so the environment that creates the obligation is structurally incapable of revealing it. | **gate** (re-homed from guardrail) — [`.xal/gate-inputs`](../.xal/gate-inputs) declares every input; `.xal/check-gate-inputs.sh` derives what `scripts/check.sh` actually requires, fails on anything undeclared, and asserts every caller workflow supplies it. It requires no inputs of its own, deliberately. | promoted→ wired as the FIRST gate in [`scripts/check.sh`](../scripts/check.sh); the 2026-08-15 failure was replayed by deleting the npm install from `ci.yml` and now fails locally before push. Canonical copy lives in `xcos-core` — do not edit `.xal/check-gate-inputs.sh` in place. Reasoning in [xal-company ADR-0005](https://github.com/xodeeq/xal-company/blob/main/docs/adr/0005-declared-gate-input-contract.md)
2026-08-12 | Sync scope, `.claude/` is a seed-time copy that nothing checks | `sync/manifest` covers `spec/` → `docs/platform/` and NOTHING else, so everything under `scaffold/.claude/` (skills, `/begin-session`, `/wrap-session`) is a one-time `cp -r` at service creation that `--check` never inspects. It will therefore drift silently and permanently across auth and `xal-registry-api` — and it is already drifting: auth's `.claude/commands/wrap-session.md` and `begin-session.md` restate the ritual that `spec/session-ritual.md` now owns canonically, with no mechanism to notice when the two disagree. XCOS §6.2 makes this worse by proposing to add `.claude/agents/` (`implementer`, `test-writer`, `contract-reviewer`, `docs-writer`) "in each service repo", which bypasses ADR-0001 entirely: per-repo agent definitions are exactly the copy-paste drift the platform repo exists to stop. Agent definitions belong in `scaffold/.claude/agents/` like every other seeded asset. | **ADR** (platform) — decide between: (a) EXTEND the sync model to cover `.claude/` (needs a second manifest or a manifest with destination paths, plus a rule for the parts a service legitimately customizes — auth's `/wrap-session` names `scripts/check.sh` and its own grep, which a Rust service must not inherit verbatim); (b) SPLIT into a synced normative core + an explicitly service-owned overlay; or (c) ACCEPT the drift deliberately and say so, so nobody assumes `--check` covers it. Whichever wins, `scaffold/.claude/agents/` is where agent definitions go. | **promoted→ RESOLVED 2026-08-15** by [xal-company ADR-0004](https://github.com/xodeeq/xal-company/blob/main/docs/adr/0004-agent-definition-distribution.md), which takes option (b): a synced normative core plus an explicitly service-owned overlay. Shared definitions are distributed as **Claude Code plugins** rather than by extending `sync/manifest` — a marketplace gives version pinning and one-way sync, which a file copy never did. This repo is now a marketplace: `.claude-plugin/marketplace.json` publishes `xal-platform-conventions`, and auth consumes it instead of holding its own copy ([xal-platform#5](https://github.com/xodeeq/xal-platform/pull/5)). The split the entry called for is real and enforced: the plugin carries only language-agnostic content, while each repo keeps its gate command and its language-specific patterns locally. The XCOS §6.2 hazard is closed too — `contract-reviewer` and `docs-writer` are named as plugin-distributed, so no per-repo agent definitions are added. They remain unwritten and are tracked as board items, which is why this is resolved as a DISTRIBUTION decision, not as content delivered. **PLATFORM RECORD LANDED 2026-09-17 as [ADR-0007](../adr/0007-claude-config-sync-boundary.md).** This entry called for a *platform* ADR and got a *service-repo* one — xal-company ADR-0004 binds xal-company, while the rule binds every repo including ones not yet created, and `xal-company/CLAUDE.md` spent a month citing an ADR that did not exist as the authority for a standing prohibition. Same shape as this ledger's own "citing a gate is not having one", one layer up: a rule whose authority is a missing document. ADR-0007 takes option (b) as ADR-0004 built it, adds nothing new to the mechanism, and its only substantive addition is stating the pinning defect as a live risk rather than as ADR-0004 §5's untrue claim that a breaking change cannot land everywhere at once
2026-08-12 | Frontmatter schema is a PLATFORM SPEC change, not a company-repo change | `frontmatter-schema.md` (XCOS design doc) mandates required YAML frontmatter keys on every doc. But the ADR format, the concept-note format and the session/handoff format are **canonical in `spec/`** (`adr-discipline.md`, `adr-template.md`, `concept-note-structure.md`, `session-ritual.md`), so mandating frontmatter on them is an additive obligation on every consuming service — a spec change, propagated by `VERSION`, not something a company repo can decree unilaterally. Scale check: **zero of auth's ~45 docs have frontmatter today**, and `xal-registry-api` will inherit whatever is decided. Enabling a checker before the spec and the backfill land would red every service's gate at once. | **ADR** (platform), then a strict ordering when it is picked up: (1) platform ADR accepting the schema → (2) update `spec/adr-discipline.md`, `spec/concept-note-structure.md`, `spec/session-ritual.md` with the required keys → (3) bump `VERSION` **minor** (additive obligation) → (4) each service syncs and backfills its docs in ONE PR → (5) only then enable `frontmatter-check.sh` as a gate. Never enable the checker before step 4 completes in that service | open — surfaced during the auth-as-consumer retrofit (auth session 17)
2026-08-12 | The wikilink ban already has a false positive, and vendoring DOUBLED it | `frontmatter-schema.md` rule 4 fails the build on "any `[[wikilink]]` syntax found anywhere in the repo". `spec/deployment-conventions.md:209` contains `` `[[http_service.checks]]` `` — TOML array-of-tables syntax naming a real `fly.toml` construct, inside **inline code on a prose line** (verified: zero fence markers precede it, so it is NOT in a fenced block — a fenced-block-only skip would still fail it). It is the only `[[…]]` in either repo, and because auth now vendors the spec it exists in **two** places: the canonical `spec/deployment-conventions.md:209` and the read-only copy at `xal-auth/docs/platform/deployment-conventions.md:209`. A naive checker fails auth for text auth is forbidden to edit. | guardrail (checker implementation) — `frontmatter-check.sh` rule 4 must strip **fenced blocks AND inline-code spans** before scanning; additionally, a checker run inside a consumer must skip `docs/platform/` entirely, since those files are read-only and a finding there is unfixable in that repo | open — do not implement rule 4 as a bare `grep -r '\[\['`
2026-08-12 | Vendored spec carries links that dangle in every consumer | `spec/README.md` links `../sync/SYNC.md`, `../VERSION` and `../CLAUDE.md` — all correct inside this repo, all broken once the file is vendored to a consumer's `docs/platform/README.md`, where `../` resolves to the consumer's `docs/`. Found by a link check immediately after auth's first real sync; it will hit `xal-registry-api` identically. The general rule: **a file in `sync/manifest` may only use relative links that stay INSIDE the vendored set** (siblings like `service-conventions.md` are fine); anything pointing outside must be an absolute URL to this repo, or not a link at all. | script (add a link check to the sync round-trip so a manifest file with an escaping relative link fails here, not in a consumer) + a `spec/` fix | **promoted→ CLOSED 2026-08-12 (session 18)**. Fixed in [xal-platform#2](https://github.com/xodeeq/xal-platform/pull/2) as predicted — its own PR with a **patch** bump to `0.1.1` — and received by auth in [xal-auth#20](https://github.com/xodeeq/xal-auth/pull/20). The three links now point at absolute `https://github.com/xodeeq/xal-platform/blob/main/...` URLs (legitimate now the repo is public), and the link TEXT moved too, since each used the path as its label. An audit of all eight manifest files confirmed these three were the complete set. The rule is now written into `sync/SYNC.md` under "What's in scope to sync" and **mechanically enforced by `scripts/check.sh` gate 2**, which has a regression test that reintroduces this exact bug and fails. Verified where it actually mattered — from the consumer's path, auth's `docs/platform/` now has **zero** dangling links (12 relative links, all resolving), down from three. This entry also became the payload that proved the drift loop end to end: auth's gate 7 went red (`pinned 0.1.0 != platform 0.1.1`, both changed files named), synced, then green — the mechanism observed rather than asserted
2026-08-12 | Repository visibility: `xal-platform` was made PUBLIC, and that is a load-bearing decision nothing records | The auth-as-consumer retrofit needed `platform-sync.sh --check` to run in auth's CI, which needs the platform repo checked out there. A GitHub Actions run for `xal-auth` gets a `GITHUB_TOKEN` scoped to `xal-auth` only, so with a PRIVATE platform repo the second `actions/checkout` fails without extra credentials. Four options were weighed: (a) **make the repo public** — no credential exists to leak, expire or rotate, and public repos get unlimited Actions minutes (private ones get 2,000/mo, which matters once XCOS crons land); (b) **deploy key** — works, keeps the spec private, but is per-consuming-repo forever (auth AND `xal-registry-api` AND every future service) and each needs rotation; (c) **fine-grained PAT** — expires in ≤ 1 year and then reds a gate that has nothing to do with the code that broke, months later, silently; (d) **skip the gate when `CI=true`** — breaks the "green locally = green in CI" invariant `scripts/check.sh` promises in its own header, on exactly the check least likely to be run by hand. (a) was chosen 2026-08-12. Evidence gathered BEFORE flipping, not after: full history scan — one commit, 29 files, all markdown and one shell script; every hit for `secret|token|key|password` was prose ABOUT secrets hygiene, zero values; no product architecture, no data model, no infrastructure identifiers. Publishing also aligns with XCOS §6.5, which intends to publish engineering guides from this material anyway. | **ADR** (platform) — this binds every service's CI and belongs in `adr/` per that README's rule ("a decision lands here only when it binds every service or the platform's own machinery"); suggested `adr/0003-repository-visibility.md` | open — the decision is MADE and ACTED ON; only its record is missing. **Reversal trigger, and it is not symmetric:** if a `spec/` file ever must carry genuinely non-public content (a named customer, a sensitive control detail, commercial terms), the repo does not simply flip back — going private re-imposes the credential problem on EVERY consumer at once, so the migration is: add a deploy key per consuming repo, wire `ssh-key:` into each `actions/checkout`, and accept per-repo key rotation as standing cost. Note also that anything already published may already have been cloned, so flipping back protects future content, not past content. The cheaper answer is usually to keep the sensitive thing out of `spec/` — it is a language-agnostic contract surface and rarely has a legitimate reason to hold secrets
2026-09-16 | A gate script cannot prove itself without recursing, so its fixtures need a second caller | ADR-0006 makes every seeded repo carry `scripts/check.sh` AND committed fixtures proving it can fail. The obvious shape — a gate inside `check.sh` that runs the fixtures — recurses forever, because proving the chain means running the chain. Three shapes were considered and two are traps: a gate that runs a REDUCED chain proves something other than what CI runs; an env-var recursion guard makes the fixture run structurally different from the real run, which is exactly the difference a fixture exists to rule out. The shape that works is a harness invoked by `ci.yml` as its own step, which copies the repo, overlays one fixture, and runs THAT copy's `check.sh` — the real script, the whole chain, in a tree that differs by one file. **Bole check** — searched by root cause. The nearest neighbour is the 2026-09-08 xal-company entry "the repo that DEFINES a check is the last one to run it": both are about a check and its subject being the same thing. NOT a merge — that one is about a repo forgetting to run a check it owns, this one is about a check that *cannot* run on itself without infinite regress. Different failure, and the fixes do not overlap | **ADR + script** ([adr/0006-repo-seeding.md](../adr/0006-repo-seeding.md) §4 records the reasoning; `scaffold/lang/go/gates/check.test.sh` is the shape, and `scripts/check-seed-set.sh` RULE 3 fails any language overlay whose `ci.yml` does not invoke both the gate and the harness — so the pattern cannot be dropped by the next overlay author) | **promoted→ 2026-09-16.** Verified on real CI, not asserted: [xal-org run 35085906470](https://github.com/xodeeq/xal-org/actions/runs/35085906470) — `failing-gofmt` exit 1 naming the format gate, `failing-coverage` exit 1 naming the coverage gate, `clean` exit 0
2026-09-16 | `gofmt` does not honour the go tool's `_`-prefix ignore rule, so "invisible to the build" is not "invisible to the gate" | The Go fixture tree holds a deliberately broken package. Putting it under `gates/_fixtures/` keeps it out of `./...` — the go tool ignores directories beginning with `_` or `.` — so `go vet`, `go build` and `go test` never see it. `gofmt` has no such rule: it walks the filesystem, so `gofmt -l .` reports the repo's own fixtures as failures and the format gate is red on a clean tree. The fix is that the format gate drives gofmt from an explicit `find` that prunes `gates/`, rather than pointing it at `.`. The general shape is worth more than the instance: **"the build ignores it" and "every tool ignores it" are different claims**, and a fixture tree is exactly where they come apart, because its whole purpose is to be broken. Any future overlay that hides fixtures behind a naming convention has to check that convention against EVERY tool in its gate chain, one at a time | **script** (the prune lives in `scaffold/lang/go/scripts/check.sh`'s `source_go_files`, with a GOTCHA comment stating why; the same function also refuses to run over an empty file list, because BSD `xargs` has no `-r` and would hang on stdin) | **promoted→ 2026-09-16.** Caught before push by running the gate in a container, not by CI — which is the only reason it is a comment and not an outage
2026-09-14 | A replacement passage drafted in another repo cannot see the gates of the repo it lands in | The xal-org spec's Appendix A.2 carried verbatim replacement text for `spec/service-conventions.md` §7. Two of its lines could not be admitted as written, and both were found by running `scripts/check.sh`, not by reading. (1) It links `[platform ADR-0004](../adr/0004-async-first-interaction-model.md)` — and `service-conventions.md` is in `sync/manifest`, so gate 2's vendored-set rule fails it: inside a consumer's `docs/platform/`, `../` means that service's `docs/`. The same paragraph already links ADR-0002 by absolute URL for exactly this reason, so the draft is internally inconsistent — the author knew the rule for one link and not the adjacent one. Converted to the absolute form. (2) `- **Auth ref:** unchanged.` is an *instruction about the existing bullet*, not text to write; splicing it literally would have deleted four real file paths (`EventEnvelope.cs`, `IDomainEvent.cs`, `AuthEvents.cs`, `EventPublisher.cs`) and replaced them with the word "unchanged". Carried the original bullet forward verbatim instead. The general shape: a passage drafted for repo A and landed in repo B is checked by B's gates and written against A's knowledge, and "verbatim" is therefore a property that has to be *tested*, not assumed — the failing run is the artifact, and both transformations were flagged in the PR rather than made silently. | guardrail (when drafting replacement text for a vendored `spec/` file from outside this repo, use absolute `https://github.com/xodeeq/xal-platform/...` URLs for every cross-tree link, and never write an editing instruction — "unchanged", "as before", "keep" — into text that will be spliced) | open — the durable fix is for the drafting side to run the target repo's gate before proposing text; that is only possible once a spec's appendices are addressable, which `xal-company/specs/admitted/` now makes true
```
