# Session 01 — Platform repo bootstrapped (extracted from auth, ADR-0009)

_Date: 2026-06-18 · Branch: main · PR: pending_

The `xal/platform` repo's first session: it was **born this session** out of the auth service
(auth session 16), realizing auth ADR-0009's deferred "dedicated `platform` repo + template/sync"
direction. This handoff is the entry point for future platform-repo work — the session ritual
(`spec/session-ritual.md`) now continues here, not only in auth.

## What changed

- Repo created (`git init`, `main`) with `README.md`, `CLAUDE.md`, `VERSION` (`0.1.0`).
- `spec/` — the canonical language-agnostic assets (process guide, service + deployment
  conventions, ADR discipline + template, concept-note structure, session ritual) + index.
- `adr/` — `README.md` + **ADR-0001** (platform repo structure + template/sync model).
- `scaffold/` — the new-service seed (CLAUDE.md.template, docs structure, ritual commands, skill
  stubs pointing at the vendored spec, read-only `docs/platform/` landing dir).
- `sync/` — `manifest`, `platform-sync.sh` (vendor + `--check`), `SYNC.md`.

## Decisions & lessons captured

- **Scope = repo + scaffold first; auth keeps its originals** (auth becomes a consumer later).
- **Fully xal-native**; a generic export is a separate future effort.
- **Sync = vendored read-only copies + pinned `VERSION`, drift surfaced as a reviewable diff**
  (ADR-0001). Spec stays language-agnostic — a grep guard for .NET-isms lives in `CLAUDE.md`.
- No `docs/lessons.md` open entries yet (none created this session).

## Gate status

- No code gate (docs/tooling/process repo). Tooling verified: `bash -n sync/platform-sync.sh`
  passes; sync round-trip + `--check` drift + scaffold self-consistency exercised on a throwaway
  consumer.

## Comprehension checks

Posed interactively (5 questions) in the shared auth session-16 wrap; full Q&A is recorded
in `auth/docs/sessions/16.md`. Summary: Q1a (why submodules/packages rejected) ✓; Q1b (where
drift is made visible) self-flagged as unknown, then walked — `--check` is a filesystem
`diff` of vendored copies + pinned `VERSION` against a platform-repo path, exit 1 → red gate
→ re-sync → PR diff is the review; Q2 (direct edit of a vendored file fails `--check`, can't
propagate) ✓; Q3 (cite-not-mandate keeps the spec language-agnostic / polyglot) ✓; Q4
(retrofit deletes `xal/docs/process-guide.md`, `--check` enforces ongoing sync) ✓; Q5
**corrected** — addendum-vs-supersede is governed by *fulfilled vs. reversed*, not
done-vs-not-done (0009 was fulfilled → addendum; a new number is for a change of direction).

## What's next

- **Auth-as-consumer retrofit** (ADR-0001 Consequences §1) — the first real exercise of
  `platform-sync.sh` against a live service.
- **Lift `infra/observability` Terraform modules** into a reusable `scaffold/` module.
- Set the platform `VERSION` bump policy into motion the first time a `spec/` change must
  propagate (see `CLAUDE.md`).

## Open questions / blockers

- **Initial commit / GitHub remote** pending the user's call (repo has no commit yet; optional
  `xal-platform` remote). No technical blockers; no ADRs awaiting approval.
